// Local domain-facing re-export of the sample-lifecycle union owned by
// `nirs4all-ui/lab`. Keep the app model importing this facade so the contract
// boundary stays explicit.
export type { SampleStatus } from 'nirs4all-ui/lab';
